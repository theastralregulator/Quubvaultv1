rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // User personal storage folder rules
    match /users/{userId}/{allPaths=**} {
      // Allow read, write, and delete if the authenticated user matches the owner UID
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
